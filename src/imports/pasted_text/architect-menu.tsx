Use the existing refined CONTRACTOR hamburger menu system in SITEFLOW as the exact base reference.

Create the ARCHITECT version of that same hamburger menu system.

IMPORTANT:
This is NOT a redesign.
Do NOT invent a new menu pattern.
Do NOT change the existing SITEFLOW menu UI language.
This is an architect-specific adaptation of the already-corrected contractor hamburger menu.

The ARCHITECT hamburger menu must replicate the CONTRACTOR menu system exactly in:
* drawer structure
* side drawer animation
* dim overlay behavior
* spacing
* typography
* visual styling
* row layout
* removal of left-side icons
* increased vertical menu spacing
* right-side chevrons/arrows
* click behavior
* smooth transitions
* premium layered motion
* image rendering reliability
* overall SITEFLOW consistency

Only change what is necessary for the ARCHITECT flow.

CRITICAL RULE:
Do NOT redesign or alter existing hierarchy. Extend the current SITEFLOW design system only.

MENU SYSTEM BEHAVIOR

When clicking the hamburger icon in the SITEFLOW header:
* a side drawer slides in smoothly from the LEFT
* background content dims softly
* the current screen remains visible behind the drawer
* animation must feel native and premium
* smooth easing
* layered motion
* no harsh sliding
* no full-page replacement

Closing behavior:
* drawer slides back left
* dim fades away
* current app state remains preserved

This must work smoothly across the ARCHITECT primary screens using the standard SITEFLOW top header, such as:
* Home
* Decisions
* Issues
* Timeline

Do NOT apply this drawer to back-button detail screens unless intentionally designed there.

USE THE REFINED MENU VERSION

The architect menu must follow the already-refined contractor version, meaning:

1. NO LEFT-SIDE MENU ICONS
Remove menu icons from the left side of all architect menu rows.
Keep:
* text labels
* right-side chevrons/arrows
* existing typography hierarchy
* clean spacing
* click behavior

2. INCREASED MENU SPACING
Use the cleaner refined spacing from the contractor version:
* more vertical breathing room between rows
* slightly increased padding / line spacing inside each row
* calmer, cleaner, more premium feel
* do not enlarge text
* do not change drawer width
* do not alter colors

PROFILE / ROLE SECTION

Top section contains:
* circular avatar
* user name
* role text underneath

IMPORTANT:
Role text MUST automatically update based on the existing “WHO YOU ARE” onboarding selection already present in the app.

ONLY use these existing roles:
* Contractor
* Architect
* Client
* Worker

For this architect version, when Architect is the active selected role:
→ menu shows “ARCHITECT”

Do NOT create new profile creation logic.
Do NOT create new account logic.
This is only reflecting the already-existing role system.

MENU ITEMS FOR ARCHITECT

The ARCHITECT hamburger menu contains:

* SWITCH PROFILE
* BLUEPRINTS & DOCUMENTS
* COMPLETED TASKS
* INVITE
* SETTINGS

Every item must be clickable and connected to a proper working prototype flow.
No dead buttons.
No static placeholders.

1. SWITCH PROFILE

Behavior:
* directly opens the existing “WHO YOU ARE” screen
* user can switch role there
* selecting a role updates:
  - menu role label
  - available architect/client/worker/contractor flows
  - accessible dashboard logic

IMPORTANT:
* do NOT create account creation flow
* user is already logged in
* this is only role switching
* preserve smooth navigation behavior

2. BLUEPRINTS & DOCUMENTS

This replaces the contractor’s “WORKER DASHBOARD” item.

Create a very clean, architect-appropriate page using the exact same SITEFLOW UI system and prototype logic as the contractor menu extensions.

This page should contain:
* a list of necessary project documents
* a large main section for viewing blueprint / drawing documents

Keep it simple and useful for architect review.
Do NOT make it overly technical or enterprise-heavy.

Content direction:
* document list cards or rows for key files
* a prominent larger blueprint viewing area / blueprint preview section
* architect-friendly review feel
* same SITEFLOW card language
* soft rounded cards
* clean spacing
* same contractor-menu extension quality and layout consistency

Do NOT:
* build a complex file manager
* add unnecessary filters
* add technical BIM-style complexity
* overdesign the page

The page should feel:
* structured
* clean
* visual
* review-oriented
* native to the Architect flow

3. COMPLETED TASKS

Purpose:
Shows a clean log of completed tasks.

Keep it simple.

Each task card contains:
* task title
* completion date
* timestamp
* approved by

Optional:
* small image thumbnail reused from existing task / construction images if title matches

Do NOT add:
* analytics
* charts
* unnecessary details
* advanced filtering

Cards should open simple detail views using the existing SITEFLOW task/detail design language.

4. INVITE

Create a simple invite system screen.

Contains 3 invite options:
* Invite Client
* Invite Architect
* Invite Worker

When clicking one:
* generate invite link UI
* allow “Copy Link”
* show confirmation toast

Examples:
* Invite Client → creates client access link to the project
* Invite Architect → architect access link
* Invite Worker → worker access link

IMPORTANT:
* no real backend logic required
* prototype flow only

Toast behavior:
* small top toast
* smooth animation
* green confirmation state
* “Link copied”
* same SITEFLOW feedback language

5. SETTINGS

Simple settings screen only.

Contains:
* language
* notifications
* theme preferences
* account info
* logout

Keep visually minimal and consistent.

IMAGE RENDERING RELIABILITY

Use the same corrected image behavior as the refined contractor hamburger menu.

Images must render reliably in all architect menu flows and linked screens.

Reuse the existing images already used throughout the SITEFLOW app wherever relevant.

For Blueprints & Documents:
* reuse existing blueprint / drawing / project visual assets if already present
* if related document visuals exist in the app, reuse them
* do not introduce broken image blocks
* do not create unnecessary unrelated assets

For Completed Tasks and related detail screens:
* reuse existing task / construction images already used in tasks, timeline, issues, and completed work flows

IMPORTANT TECHNICAL REQUIREMENT

Fix / preserve the image rendering architecture so images:
* always load
* persist between transitions
* survive screen changes
* remain visible after interactions
* render consistently across all architect menu flows

No more:
* blank avatars
* missing thumbnails
* broken image states
* placeholder artifacts
* grey fills where images should appear

PROTOTYPE REQUIREMENTS

Everything must function as a connected prototype.

This includes:
* smooth drawer open / close behavior
* correct navigation
* working back flows
* correct role switching
* working invite toasts
* working clickable menu items
* working Blueprints & Documents flow
* working Completed Tasks flow
* current app state preserved behind drawer

Do NOT:
* break existing architect navigation
* reset app state unnecessarily
* replace current animation system
* redesign existing screens

DESIGN SYSTEM REQUIREMENTS

Keep consistent:
* SITEFLOW typography
* spacing
* corner radius
* shadows
* colors
* icons already used elsewhere in the app
* animation style
* card language

The architect hamburger menu system must feel like a natural built-in extension of the existing app and must visually match the corrected contractor version exactly.

IMPORTANT ARCHITECTURE RULE

The CONTRACTOR hamburger menu is the structural base.
The ARCHITECT version must reuse:
* the same drawer
* the same motion
* the same visual system
* the same refined spacing
* the same minimal row style without left icons

Only the menu contents and linked flows should differ where architect-specific behavior is needed.

Do NOT:
* redesign the drawer
* alter the animation language
* change the drawer width
* change the refined spacing style
* add left-side menu icons back
* break future stakeholder-specific modular reuse

Final goal:
Create the ARCHITECT hamburger menu in the exact same refined format as the corrected contractor hamburger menu, with the same UI, same motion, same prototype behavior, same spacing refinements, and same image reliability — but replace WORKER DASHBOARD with BLUEPRINTS & DOCUMENTS and connect that new architect-specific flow cleanly.